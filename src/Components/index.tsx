import React from 'react';
import Header from './Module/Header/Header.component';

const WebContent = () => {
    return (
        <div className="grid">
            <Header/>
        </div>
    );
}

export default WebContent;